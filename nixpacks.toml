[phases.setup]
aptPkgs = ['build-essential', 'curl']

[phases.install]
cmds = [
  'curl --proto "=https" --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y',
  'PATH="/root/.cargo/bin:$PATH" python -m venv --copies /opt/venv',
  '. /opt/venv/bin/activate && pip install -r requirements.txt'
]